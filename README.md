# 🌌 AniHarvest Omni Archive V15

**The definitive, high-velocity metadata extraction suite for AniList.**

![License](https://img.shields.io/badge/License-MIT-purple.svg)
![Protocol](https://img.shields.io/badge/Protocol-Omni_Archival-blue.svg)
![Status](https://img.shields.io/badge/Status-Stable-green.svg)
![Easter Eggs](https://img.shields.io/badge/Secrets-Found-yellow.svg)

---

## 🚀 The Omni Station
AniHarvest V15 is an enterprise-grade utility designed to scrape, verify, and persist the entire AniList database. It features a stunning, industrial real-time command dashboard with advanced filtering and safety protocols.

### 🛠️ Hardware-Grade Safety
- **Temporal Tunneling**: Target specific eras by setting `Min Year` and `Max Year`. The engine skips any entry outside your target scope.
- **Storage Sentinel**: Set a critical disk floor (e.g., 5GB). The engine will trigger an emergency lockdown if your device hits the limit.
- **State Persistence**: Toggleable session resumption. Continue from the exact millisecond you left off.

### 💀 Operations Matrix
- **⚖️ LEGAL**: 100% compliant stealth extraction (30 RPM).
- **⚡ BURST**: High-throughput harvesting (90 RPM).
- **💀 GHOST**: 25 concurrent workers, IP spoofing, and complexity evasion.
- **🌐 DOM**: Selenium-grade Chrome fallback for scraping the official web frontend.

## 💎 Features & Secrets
- **Glassmorphism HUD**: A tactile, resizable terminal and card stream with real-time telemetry.
- **Knowledge Graph**: Automatically extracts and maps `anime_relations` into a structured PostgreSQL graph.
- **Strike Overlay**: Visual countdown and ip-recalibration during rate-limit events.
- **Easter Eggs**: 
  - Typing `matrix` enables the digital rain.
  - Typing `cyber` enables the pink neon theme.
  - Multi-clicking the logo triggers agentic theme overrides and vessel evolution.
  - Tactical "Barrel Roll" utility.

## ⚙️ Deployment

1. **Clone & Install**:
   ```bash
   npm install
   ```
2. **Environment Configuration**:
   Create a `.env` file (see `.env.example`):
   ```env
   DATABASE_URL=postgres://user:pass@host:port/db
   MAX_ID=650000
   PORT=3000
   ```
3. **Ignition**:
   ```bash
   npm start
   ```

---
**Build by Antigravity // Deepmind Advanced Agentic Coding Team.**
`scraper`, `anilist`, `graphql`, `industrial`, `ghost-protocol`, `data-recovery`, `typescript`, `dashboard`.
